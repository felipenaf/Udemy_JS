/**
 * Faça um programa que leia 2 números, 
 * some-os e exiba uma mensagem com o resultado comparando se os valores são maiores, 
 * menores ou iguais a 100
*/

function soma(n1, n2){
    let soma = n1 + n2
    
    return `O resultado é ${soma}`
}

console.log(soma(50, 51))